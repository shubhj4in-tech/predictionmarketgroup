// Phase 4: Group detail + market list + leaderboard
export default function GroupPage({ params }: { params: { groupId: string } }) {
  return <div className="p-6">Group {params.groupId} — Phase 4</div>;
}
