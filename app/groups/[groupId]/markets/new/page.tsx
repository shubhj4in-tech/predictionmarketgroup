// Phase 4: Create new market form
export default function NewMarketPage({ params }: { params: { groupId: string } }) {
  return <div className="p-6">New Market in group {params.groupId} — Phase 4</div>;
}
